services:
  - type: web
    name: verizon-gps-webhook
    runtime: python

    buildCommand: pip install -r requirements.txt

    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000

    envVars:
      - key: WEBHOOK_USERNAME
        sync: false

      - key: WEBHOOK_PASSWORD
        sync: false